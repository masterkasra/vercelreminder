// بعد از اضافه کردن کلاس جدید در index.html دستور `npm run build:css` را اجرا کنید تا styles.css به‌روز شود
module.exports = {
  darkMode: 'class',
  content: ['./index.html'],
  theme: { extend: {} }
};
